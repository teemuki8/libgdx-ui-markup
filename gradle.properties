# Strict dependency verification: every resolved artifact (project, plugin, settings,
# buildscript, and tooling graphs) must match gradle/verification-metadata.xml or the
# build fails before task execution. `verbose` surfaces the per-artifact verification
# status in CI logs instead of collapsing it to a one-line summary.
org.gradle.dependency.verification=strict
org.gradle.dependency.verification.console=verbose
