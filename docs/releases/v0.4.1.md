# libgdx-ui-markup 0.4.1

## Highlights

- Qualifies the adapters against the current harness 1.2.0 and agent-runtime 2.0.0 releases.
- Retains executable compatibility coverage for harness 1.1.0 and agent-runtime 1.0.0.
- Runs both profiles from exact, independently committed dependency locks in CI and release.
- Preserves the authoritative runtime comparison proof: independent UI/domain divergence reports
  `MISMATCH`; widget mirror remains preview-only transport evidence.

## Published modules

- `io.github.teemuki8:libgdx-ui-markup:0.4.1`
- `io.github.teemuki8:libgdx-ui-markup-harness:0.4.1`
- `io.github.teemuki8:libgdx-ui-markup-runtime:0.4.1`

The preview, qualification runner, and IDEA plugin remain unpublished.

## Verification

```bash
python3 scripts/test-ecosystem-profile.py
xvfb-run -a ./gradlew minimumEcosystemTest currentEcosystemTest --warning-mode=fail
xvfb-run -a ./gradlew clean check --warning-mode=fail
xvfb-run -a ./gradlew :libgdx-ui-markup-qualification:test \
  -PstrictQualification=true --warning-mode=fail
./gradlew :libgdx-ui-markup-idea:buildPlugin --warning-mode=fail
```
